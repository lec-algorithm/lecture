allowBuilds:
  esbuild: true
  sharp: true
minimumReleaseAgeExclude:
  - stack-site-builder
# pnpm 10+ no longer runs dependency build scripts unless explicitly allowed.
# Astro needs esbuild (bundling) and sharp (image optimization) to build.
onlyBuiltDependencies:
  - esbuild
  - sharp
