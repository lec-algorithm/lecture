// @ts-check
/**
 * `[[용어]]` 위키링크의 중앙 용어집. 본문 어디서든 `[[big-o]]`,
 * `[[big-o|Big-O]]`처럼 참조하면 빌드 시 해당 항목으로 링크된다. 등록되지 않은
 * 용어는 빌드가 실패한다(의도된 동작). 항목 형태:
 *  - { label, def }              — 정의만 있는 용어 (용어집 페이지에 표시)
 *  - { label, course: '<slug>' } — 강의 문서로 연결
 *  - { label, article: '<slug>' } — 글로 연결
 *  - { label, href }             — 외부 링크
 *
 * 주제 자료를 만들면서 필요한 용어를 그때그때 추가한다. 여기 있는 것은
 * 과정 전체에서 반복해서 쓰이는 기본 용어다.
 */
export const glossary = {
  algorithm: {
    label: '알고리즘',
    def: '문제를 푸는 절차를 유한한 단계로 명확히 적은 것. 같은 입력에 같은 출력을 내고, 반드시 끝나야 한다.',
  },
  'pseudo-code': {
    label: '의사코드',
    def: '특정 언어의 문법에 매이지 않고 알고리즘의 절차만 적은 코드. 이 과정에서는 의사코드가 기준이고 C와 Python 구현은 그것을 옮긴 것이다.',
  },
  'big-o': {
    label: 'Big-O',
    def: '입력 크기가 커질 때 실행 시간이나 메모리가 어떤 비율로 늘어나는지를 나타내는 점근 상한. 상수와 낮은 차수 항을 버리고 증가율만 남긴다.',
  },
  'asymptotic-analysis': {
    label: '점근 분석',
    def: '입력이 충분히 커졌을 때의 동작만 따지는 분석. 하드웨어나 언어에 좌우되는 상수를 걷어내고 알고리즘 자체를 비교하기 위한 도구다.',
  },
  'time-complexity': {
    label: '시간 복잡도',
    def: '입력 크기에 대한 연산 횟수의 증가율. 실제 초 단위 시간이 아니라 비교·이동 같은 기본 연산의 개수를 센다.',
  },
  'space-complexity': {
    label: '공간 복잡도',
    def: '입력 크기에 대해 추가로 필요한 메모리의 증가율. 제자리 정렬과 그렇지 않은 정렬을 가르는 기준이다.',
  },
  'divide-and-conquer': {
    label: '분할 정복',
    def: '문제를 같은 형태의 작은 문제로 쪼개고(divide), 각각을 풀고(conquer), 결과를 합치는(combine) 설계 전략. 머지 정렬과 퀵 정렬이 대표적이다.',
  },
  recursion: {
    label: '재귀',
    def: '함수가 자기 자신을 부르며 문제를 줄여 가는 방식. 종료 조건과 축소 단계가 있어야 끝난다.',
  },
  'stable-sort': {
    label: '안정 정렬',
    def: '같은 값을 가진 원소들의 원래 순서가 정렬 후에도 유지되는 정렬. 여러 기준으로 연달아 정렬할 때 의미가 생긴다.',
  },
  'in-place': {
    label: '제자리',
    def: '입력 배열 바깥에 크기가 입력에 비례하는 추가 공간을 쓰지 않는 성질. 퀵 정렬은 제자리지만 머지 정렬은 아니다.',
  },
  pivot: {
    label: '피벗',
    def: '퀵 정렬에서 배열을 두 덩어리로 가르는 기준값. 어떻게 고르느냐가 최악의 경우를 만드느냐 피하느냐를 결정한다.',
  },
  'greedy-algorithm': {
    label: '그리디 알고리즘',
    def: '매 단계에서 그 순간 가장 좋아 보이는 선택을 하고 되돌리지 않는 방식. 문제가 조건을 만족할 때만 전체 최적해가 보장된다.',
  },
  'dynamic-programming': {
    label: '동적 계획법',
    def: '겹치는 부분 문제의 답을 한 번만 계산하고 저장해 재사용하는 방식. 지수 시간으로 부풀던 재귀를 다항 시간으로 되돌린다.',
  },
  memoization: {
    label: '메모이제이션',
    def: '재귀를 그대로 두고 이미 계산한 부분 문제의 답을 캐시에 담아 두는 하향식 동적 계획법.',
  },
  tabulation: {
    label: '타뷸레이션',
    def: '작은 부분 문제부터 표를 채워 올라가는 상향식 동적 계획법. 재귀 호출 비용이 없다.',
  },
  'hash-function': {
    label: '해시 함수',
    def: '임의의 키를 정해진 범위의 정수로 바꾸는 함수. 그 정수가 배열의 인덱스가 된다.',
  },
  collision: {
    label: '충돌',
    def: '서로 다른 키가 같은 해시값을 갖는 상황. 해시 테이블 설계의 절반은 충돌을 어떻게 다루느냐다.',
  },
  bst: {
    label: '이진 탐색 트리',
    def: '왼쪽 서브트리의 모든 키가 자기보다 작고 오른쪽이 자기보다 큰 이진 트리. 균형이 무너지면 탐색이 리스트처럼 느려진다.',
  },
  trie: {
    label: '트라이',
    def: '문자열을 문자 단위로 쪼개 경로로 저장하는 트리. 접두사를 공유하므로 접두사 검색이 자연스럽다.',
  },
  graph: {
    label: '그래프',
    def: '정점(vertex)과 그 사이를 잇는 간선(edge)으로 이루어진 자료구조. 지도, 소셜 관계, 의존성이 모두 그래프로 표현된다.',
  },
  bfs: {
    label: 'BFS',
    def: '시작점에서 가까운 정점부터 층별로 훑는 그래프 탐색. 가중치가 없는 그래프에서는 이것만으로 최단 경로가 나온다.',
  },
  dfs: {
    label: 'DFS',
    def: '한 방향으로 갈 수 있는 데까지 들어갔다가 막히면 되돌아 나오는 그래프 탐색.',
  },
  mst: {
    label: '최소 스패닝 트리',
    def: '모든 정점을 사이클 없이 잇는 부분 그래프 중 간선 가중치의 합이 가장 작은 것.',
  },
  'shortest-path': {
    label: '최단 경로',
    def: '두 정점을 잇는 경로 중 가중치의 합이 가장 작은 경로. 간선 가중치가 음수이면 Dijkstra는 쓸 수 없다.',
  },
  'priority-queue': {
    label: '우선순위 큐',
    def: '넣은 순서가 아니라 우선순위가 가장 높은 원소가 먼저 나오는 큐. 보통 힙으로 구현한다.',
  },
};
