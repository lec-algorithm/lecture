---
title: "주제 08. 해시 테이블"
description: "해시 함수, 충돌 해결의 두 방식, 그리고 성능"
date: 2026-08-25
category: data-structures
order: "9092"
level: 3
hours: "3:00"
tags: ["주제 08"]
---

> 자료는 준비 중입니다. 이 주제를 다루기 전에 이 페이지가 채워집니다.

## 다룰 내용

- 키를 배열 인덱스로 바꾼다는 발상
- 해시 함수와 모듈러 해싱
- 충돌(collision)은 왜 반드시 일어나는가
- 개별 체이닝(separate chaining): 삽입·검색·삭제
- 선형 탐지(linear probing): 삽입·검색·삭제와 삭제 후 후속 조치
- 키 무리(clustering)와 적재율

## 실습 코드

`algorithm-code` 저장소의 `src/topic-08-hashing/` 폴더에 들어갑니다.
예제마다 의사코드 하나에 C와 Python 구현이 붙습니다.

```plaintext
src/topic-08-hashing/
├── 01_hash_table_sc/
└── 02_hash_table_lp/
```
